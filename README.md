
Invest Help Bot

Телеграм-бот для котировок, графиков и конвертации валют.
Поддерживает US акции (Stooq), российские акции (MOEX ISS), крипту (CoinGecko), а TON берёт напрямую с бирж (Binance → OKX → KuCoin).
Бот работает через кнопки (inline-keyboard), команды тоже поддерживаются.

✨ Возможности
•	Котировки отслеживаемых тикеров в выбранной базе: USD / EUR / RUB
– показывается цена с символом валюты ($/€/₽) и изменение за 24ч (если доступно).
•	Графики за периоды: 7d, 30d, 90d, 1y (+ умный 1d с часовыми свечами, где это возможно).
•	Конвертер валют: инлайн-поток (выбор «из» → «в» → сумма) и парсер текста /convert 100 usd rub.
•	Липкие режимы:
•	выбрал График — можно подряд вводить TICKER [period] без повторного выбора;
•	для Конвертера после расчёта появляются кнопки «Продолжить» / «Сменить валюты» / «Назад».
•	Российские акции: автоматическое определение тикера MOEX
(SBER, GAZP, LKOH, …, а также TICKER-ME).
•	Производительность:
•	кэш результатов котировок (по умолчанию на несколько секунд);
•	кэш FX-курсов (1 час);
•	HTTP-пулинг + параллельные запросы.

🏗 Стек и источники
•	Библиотеки: python-telegram-bot (v20+), requests, matplotlib, python-dotenv (+ опционально ujson).
•	Данные:
•	US акции — Stooq (дневные CSV).
•	RU акции — MOEX ISS (/candles.json, плата TQBR).
•	Крипто — CoinGecko (/simple/price, /coins/markets, /market_chart).
•	TON — напрямую: Binance, OKX, KuCoin (цена и 24h %), без зависания на CoinGecko.


Бот работает в режиме long polling.

🧭 Управление

Кнопки (рекомендуется)
•	📊 Цены — показать котировки всех добавленных тикеров.
•	🧾 Список — текущий список тикеров.
•	➕ Добавить / ➖ Удалить — редактирование списка.
•	📈 График — войти в режим графиков (липкий режим).
•	💱 Валюта — выбрать базовую: USD / EUR / RUB (кнопка показывает символ $/€/₽).
•	🔁 Конвертер — пошаговый конвертер (липкий режим):
•	«ИЗ» → «В» → ввести сумму → результат → 🔁 Продолжить или ♻️ Сменить валюты.
•	🧹 Очистить — удалить все тикеры.

Команды (альтернатива)
•	/start — приветствие и кнопки.
•	/add AAPL NVDA BTC — добавить тикеры.
•	/remove AAPL / /clear / /list — управление списком.
•	/price — котировки.
•	/chart BTC 30d — график тикера за период (7d|30d|90d|1y; 1d отрабатывается умно).
•	/setbase USD|EUR|RUB — выбор базовой валюты.
•	/convert 100 usd rub — конвертация.
•	Поддерживаются формы: 100 usd->rub, 250 eur to rub, 100 rub usd, 100 RUB (тогда из базовой в RUB).

🏷 Тикеры
•	US: обычные тикеры, напр. AAPL, NVDA.
•	RU (MOEX): SBER, GAZP, LKOH, … или с суффиксом -ME (например, SBER-ME).
•	Крипто: BTC, ETH, TON, USDT, SOL, BNB, …
(маппинг CoinGecko задаётся в CG_ID_MAP_STATIC, при необходимости дополняй).

📈 Графики
•	Периоды: 7d, 30d, 90d, 1y.
•	Для коротких периодов используются более частые свечи (часовые, где доступно).
•	Оси подписаны символом валюты ($/€/₽).

💱 Конвертер
•	Кнопочный поток: ИЗ → В → Сумма + «🔁 Продолжить».
•	Текстовый парсер:
•	100 usd rub, 100 usd->rub, 100 usd to rub, 100 rub usd, 100 RUB.
•	Курс берётся с нескольких бесплатных источников (с кэшем и фолбэками).

⚙️ Настройки и кэш
•	PRICE_TTL — кэш агрегированных котировок (сек), по умолчанию 5 c.
•	Кэш FX-курсов — 1 час.
•	Соединения: общий requests.Session с пулом и ретраями.
•	Состояние пользователей (тикеры/валюта) хранится в data.json.

🧩 Расширение
•	Добавляй крипто-идентификаторы в CG_ID_MAP_STATIC.
•	Для MOEX — при необходимости дополняй список эвристик _RU_TICKERS.
•	Можно добавить новые провайдеры котировок/FX по аналогии с существующими функциями.

🛠 Траблшутинг
•	Conflict: terminated by other getUpdates request — запущено 2 экземпляра бота с одинаковым токеном. Оставь один.
•	401 Unauthorized на CoinGecko — исчерпан лимит. Укажи COINGECKO_API_KEY или подожди. TON у нас берётся с бирж, остальное — с CG.
•	Пустой график за 1 день — для крипты используется hourly, для акций у источников могут быть задержки/выходные. Попробуй 7d.
•	Медленно — увеличь IO_POOL, при необходимости подними PRICE_TTL.

⚠️ Дисклеймер

Бот создан для ознакомительных целей. Данные предоставляются сторонними сервисами «как есть» и не являются инвестиционной рекомендацией.