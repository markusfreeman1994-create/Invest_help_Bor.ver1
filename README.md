# Invest Help Bot — финансовый ассистент в Telegram

Удобный бот для котировок, графиков, конвертации валют и умной аналитики. Навигация — **кнопками**, без сложных команд. Поддерживает **акции (US/RU)**, **криптовалюты (включая TON)**, и **10+ базовых валют**.

> ⚠️ Бот не даёт персональных инвестиционных рекомендаций. Любые выводы — информационные.

---

## Что умеет

**Основное**
- **📊 Котировки** — выводит цену и изменение (24h) по выбранным тикерам в выбранной базовой валюте.
- **📈 Графики** — исторические графики по тикеру (дни/часы) с нормальной шкалой для 1d (почасовые точки, не пусто).
- **🔁 Конвертер валют** — простой сценарий «из / в / сумма», остаётесь в режиме конвертера до выхода.
- **🧾 Список тикеров** — добавить/удалить, очистить, хранится на пользователя.
- **💱 Базовая валюта** — USD / EUR / RUB / GBP / JPY / CHF / CNY / AUD / CAD / TRY (и можно расширять).

**Умные режимы**
- **🤖 Помощник (нейросеть)** — понимает естественный язык и **сам** запускает нужное действие:
    - `price` (цены), `chart` (график), `convert` (конвертер), `add/remove` (тикеры), `setbase` (база), `analyze` (аналитика).
    - Визуализация работы: бот показывает «**Помощник думает…**» и индикатор набора, затем — готовый ответ.
- **🧠 Аналитик** — отвечает **на ваш вопрос** (например: *«что с NVDA на 30d?»*, *«BTC и ставки ФРС?»*)
    - Вывод: компактный блок тикеров (цена, 24h, 7d/30d/90d/1y) + **краткие инсайты списком** (без бессмысленных «разделов» и повторов цифр).
    - Визуализация работы: «**Аналитик думает…**» + индикатор набора.
    - В режиме аналитика можно задавать вопросы подряд; одна кнопка — **«◀️ Назад»**.

**Поддерживаемые активы**
- **Крипта**: BTC, ETH, TON, …
- **US-акции**: AAPL, NVDA, TSLA, MSFT и др.
- **RU-акции**: SBER и др. тикеры MOEX.

---

## Источники данных и устойчивость

- **Криптовалюты** — CoinGecko API (цены/история). Для **TON** используется биржевой API, при лимитах есть запасной путь.
- **US-акции (история)** — Stooq (в USD) с пересчётом в базовую валюту.
    - Если Stooq недоступен, срабатывает **предохранитель**: повторные запросы к Stooq «замораживаются» на 5 минут, чтобы не забивать логи и не тормозить бота.
- **RU-акции** — MOEX API (история/цены) с конвертацией при необходимости.
- **FX (валютные курсы)** — внешний провайдер кросс-курсов для пересчёта базовых валют.

Принцип выбора источника в графиках: **сначала крипта**, потом **MOEX**, затем **Stooq** (для US). Это исключает ложные вызовы Stooq для крипты.

---

## Установка и запуск

### 1) Зависимости
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
```

### 2) Переменные окружения (`.env` или переменные системы)
Обязательные:
- `BOT_TOKEN` — токен Telegram-бота.

Рекомендуемые:
- `DEFAULT_BASE` — базовая валюта по умолчанию (напр. `usd`).
- `OPENAI_API_KEY` — ключ к модели для 🤖 Помощника и 🧠 Аналитика.

Необязательные:
- `OPENAI_BASE` — базовый URL API, по умолчанию `https://api.openai.com/v1`.
- `OPENAI_MODEL` — модель, по умолчанию `gpt-4o` (поддержан фолбэк на `gpt-4o-mini`, см. ниже).
- `OPENAI_ORG` / `OPENAI_PROJECT` — если требуются заголовки организации/проекта.
- `OPENAI_ALLOW_FALLBACK=1` — при ошибке доступа к `gpt-4o` бот попробует `gpt-4o-mini`.
- `COINGECKO_API_KEY` — ключ CoinGecko (если используете платный тариф/ограничения).
- Таймауты Telegram-клиента: `TG_CONNECT_TIMEOUT`, `TG_READ_TIMEOUT`, `TG_WRITE_TIMEOUT`, `TG_POOL_TIMEOUT`, `TG_UPDATES_TIMEOUT`.

### 3) Запуск локально (polling)
```bash
python Bot.py
```
В логах должно быть: `Application started`.

> Если видите `Conflict: terminated by other getUpdates request` — запущен второй экземпляр (остановите один).

---

## Интерфейс (кнопки и режимы)

- **Главное меню**:
    - «Котировки», «График», «Конвертер», «Тикеры», «Базовая валюта», **«🤖 Помощник»**, **«🧠 Аналитик»**.
- **Котировки**: бот берёт ваши сохранённые тикеры (или те, что вы добавите) и печатает **цены + 24h%**.
- **График**: отправьте тикер и период. Для 1d используется **почасовая** серия.
- **Конвертер**: выбираете «из/в», вводите сумму. После ответа бот предлагает **продолжить** конвертацию или выйти.
- **Тикеры**: добавить / удалить / очистить — всё кнопками, без команд.
- **Базовая валюта**: выберите одну из поддерживаемых; сохраняется на пользователя.
- **🤖 Помощник**: пишите в свободной форме — он либо отвечает, либо запускает нужный инструмент. Есть «Помощник думает…» + индикатор.
- **🧠 Аналитик**: задайте **вопрос** по рынку/тикеру/сравнению. Ответ: компактный **блок тикеров** + **маркеры-инсайты**. Есть «Аналитик думает…» + индикатор. Одна кнопка — «◀️ Назад».

Примеры фраз:
- «покажи цены aapl nvda btc»
- «график TON на 30d»
- «конвертируй 250 eur в rub»
- «добавь msft и покажи цены»
- «проанализируй рынок: btc и ставки фрс»

---

## Частые проблемы и решения

### Stooq (US-история) недоступен
В логах: `Stooq history failed ... Connection refused`. Это нормально при их даунтайме/блокировке. В боте включён **передохранитель** — повторные вызовы к Stooq отключаются на **5 минут**. На крипту это не влияет.

### OpenAI 403 / модель недоступна
Проверьте:
- `OPENAI_API_KEY` действителен и имеет доступ к модели `gpt-4o`;
- при необходимости задайте `OPENAI_ORG` / `OPENAI_PROJECT`;
- включите `OPENAI_ALLOW_FALLBACK=1` — бот попробует `gpt-4o-mini`.
  В логах теперь есть подробности ошибки (код/сообщение), чтобы быстро диагностировать причину.

### Таймауты Telegram при старте
Сообщение `telegram.error.TimedOut` на `get_me()`. Мы используем **расширенные таймауты** HTTPX. Проверьте сеть/VPN/прокси. Можно настроить переменные `TG_*`.

### Конфликт polling
`Conflict: terminated by other getUpdates request` — остановите лишний процесс.

---

## Примечания по приватности и ответственности
- Бот хранит минимум необходимых данных (ваши тикеры и базовую валюту) и не делится ими с третьими лицами.
- Аналитика и ответы ИИ — справочные, **не финансовый совет**.

---

## Планы на развитие
- Новости/события в контексте анализа (интеграция News API).
- Mini-App интерфейс (WebApp) для быстрых графиков и конвертера.
- Кэширование историй/котировок для ещё более мгновенных ответов.
- Команда `/model` для переключения модели из чата.

---

## Быстрый чек-лист, если «что-то не так»
- Токен бота задан? `BOT_TOKEN`
- Ключ модели задан? `OPENAI_API_KEY`
- Источники не лимитят? (429/403 в логах)
- Stooq «лежит»? (тогда подождите 5 минут — сработает предохранитель)
- Запущен только один процесс? (иначе будет конфликт polling)

Готово! Если нужно — можно включить новостной контекст для 🧠 Аналитика или добавить собственные источники котировок.